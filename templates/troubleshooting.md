# 故障现象：直接描述问题

先给出影响范围、最可能的故障层和推荐的安全排查顺序。

## 文档信息

- **技术领域**：填写产品或组件名称
- **适用范围**：填写部署方式和环境
- **适用版本**：填写已验证版本
- **风险等级**：低 / 中 / 高
- **文档状态**：草稿 / 已验证 / 已弃用
- **最后验证**：YYYY-MM-DD
- **来源**：官方文档、工单或脱敏实测记录

## 现象与影响

- 用户或监控看到的现象
- 首次发生时间
- 受影响的节点、组件和业务
- 最近发生过的变更

## 安全边界

列出排查期间禁止执行的操作，以及重启、删除、清理或修改配置前必须完成的检查和备份。

## 排查路径

按照依赖关系从底层到上层排列，例如：

```text
硬件 → 操作系统 → 容器运行时 → Kubernetes/K3s → Pod → Service → 业务
```

## 排查步骤

### 1. 检查第一层依赖

```bash
# 填写只读排查命令
```

- 预期结果：正常状态应具备的特征。
- 异常判断：哪些字段或输出可以证明故障。
- 下一步：正常时检查哪里，异常时进入哪个解决步骤。

### 2. 检查第二层依赖

```bash
# 填写只读排查命令
```

- 预期结果：填写判断标准。
- 异常判断：填写直接证据。

## 原因与解决方案

### 原因一：填写已被证据支持的原因

解决步骤：

```bash
# 填写修复命令
```

风险：说明影响范围。

回退：说明如何恢复。

## 逐层验收

按依赖顺序验证，例如 Node → Pod → Service → 业务。每一层都写出命令、预期结果和通过条件。

## 预防措施

- 建议的监控和告警
- 容量、配置或流程改进
- 需要定期执行的检查

## 升级处理

说明在什么证据或影响范围下停止操作并升级给更高层级支持，同时列出需要保留的日志和现场信息。

## 相关文档

- 添加官方故障说明和 Wiki 内部相关文章链接
